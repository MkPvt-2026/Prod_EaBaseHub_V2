/* ============================================================
   หน้าผู้จัดการอนุมัติราคาพิเศษ
   File: /js/pages/forms/special-price-manager.js
   ============================================================ */

"use strict";

document.addEventListener("DOMContentLoaded", () => {
  // ใช้สำหรับงานเฉพาะหน้าของ manager
  // ตอนนี้ปุ่มเปลี่ยนหน้าใน HTML ถูกเปลี่ยนเป็น location.href แล้ว
});
