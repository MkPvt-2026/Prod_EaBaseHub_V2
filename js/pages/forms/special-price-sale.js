/* ============================================================
   หน้าส่งคำขอราคาพิเศษ (Sale)
   File: /js/pages/forms/special-price-sale.js
   ============================================================ */

"use strict";

document.addEventListener("DOMContentLoaded", () => {
  // ใช้สำหรับงานเฉพาะหน้าของ sale
  // ตอนนี้ปุ่มเปลี่ยนหน้าใน HTML ถูกเปลี่ยนเป็น location.href แล้ว
});
