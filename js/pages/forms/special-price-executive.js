/* ============================================================
   หน้าผู้บริหารอนุมัติราคาพิเศษ
   File: /js/pages/forms/special-price-executive.js
   ============================================================ */

"use strict";

document.addEventListener("DOMContentLoaded", () => {
  // ใช้สำหรับงานเฉพาะหน้าของ executive
  // ตอนนี้ปุ่มเปลี่ยนหน้าใน HTML ถูกเปลี่ยนเป็น location.href แล้ว
});
