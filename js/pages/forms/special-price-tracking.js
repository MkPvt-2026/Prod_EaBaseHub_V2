/* ============================================================
   หน้าติดตามสถานะราคาพิเศษ
   File: /js/pages/forms/special-price-tracking.js
   ============================================================ */

"use strict";

document.addEventListener("DOMContentLoaded", () => {
  // ใช้สำหรับงานเฉพาะหน้าของ tracking
  // ตอนนี้ปุ่มเปลี่ยนหน้าใน HTML ถูกเปลี่ยนเป็น location.href แล้ว
});
